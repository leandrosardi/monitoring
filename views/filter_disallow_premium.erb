<%
begin
    aids = params[:aids].split(',')
    DB.execute("UPDATE \"account\" SET premium = false WHERE id IN ('#{aids.join("','")}')")
    redirect "/monitoring/agency/accounts?msg=Premium+Disabled"
rescue => e
    redirect "/monitoring/agency/accounts?err=Error+Disabling+Premium (#{CGI.escape(e.message)})"
end
%>