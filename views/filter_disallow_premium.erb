<%
begin
    aids = params[:aids].split(',')
    DB.execute("UPDATE \"account\" SET premium = false WHERE id IN ('#{aids.join("','")}')")
    redirect "/monitoring/production?msg=Premium+Disabled"
rescue => e
    redirect "/monitoring/production?err=Error+Disabling+Premium (#{CGI.escape(e.message)})"
end
%>